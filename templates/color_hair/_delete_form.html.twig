<form method="post" action="{{ path('color_hair_delete', {'id': color_hair.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ color_hair.id) }}">
    <button class="btn">Delete</button>
</form>
