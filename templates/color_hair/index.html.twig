{% extends 'base.html.twig' %}

{% block title %}ColorHair index{% endblock %}

{% block body %}
    <h1>ColorHair index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Color</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for color_hair in color_hairs %}
            <tr>
                <td>{{ color_hair.id }}</td>
                <td>{{ color_hair.color }}</td>
                <td>
                    <a href="{{ path('color_hair_show', {'id': color_hair.id}) }}">show</a>
                    <a href="{{ path('color_hair_edit', {'id': color_hair.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('color_hair_new') }}">Create new</a>
{% endblock %}
