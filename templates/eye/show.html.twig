{% extends 'base.html.twig' %}

{% block title %}Eye{% endblock %}

{% block body %}
    <h1>Eye</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ eye.id }}</td>
            </tr>
            <tr>
                <th>Color</th>
                <td>{{ eye.color }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('eye_index') }}">back to list</a>

    <a href="{{ path('eye_edit', {'id': eye.id}) }}">edit</a>

    {{ include('eye/_delete_form.html.twig') }}
{% endblock %}
