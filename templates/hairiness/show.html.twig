{% extends 'base.html.twig' %}

{% block title %}Hairiness{% endblock %}

{% block body %}
    <h1>Hairiness</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ hairiness.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ hairiness.type }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('hairiness_index') }}">back to list</a>

    <a href="{{ path('hairiness_edit', {'id': hairiness.id}) }}">edit</a>

    {{ include('hairiness/_delete_form.html.twig') }}
{% endblock %}
