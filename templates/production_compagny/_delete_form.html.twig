<form method="post" action="{{ path('production_compagny_delete', {'id': production_compagny.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ production_compagny.id) }}">
    <button class="btn btn-primary btn-block">Supprimer</button>
</form>
