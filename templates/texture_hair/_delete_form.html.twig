<form method="post" action="{{ path('texture_hair_delete', {'id': texture_hair.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ texture_hair.id) }}">
    <button class="btn">Delete</button>
</form>
