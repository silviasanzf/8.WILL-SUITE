{% extends 'base.html.twig' %}

{% block title %}TextureHair index{% endblock %}

{% block body %}
    <h1>TextureHair index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Type</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for texture_hair in texture_hairs %}
            <tr>
                <td>{{ texture_hair.id }}</td>
                <td>{{ texture_hair.type }}</td>
                <td>
                    <a href="{{ path('texture_hair_show', {'id': texture_hair.id}) }}">show</a>
                    <a href="{{ path('texture_hair_edit', {'id': texture_hair.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('texture_hair_new') }}">Create new</a>
{% endblock %}
